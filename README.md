# KruAi - ติวเตอร์อัจฉริยะ 🎓

แอปพลิเคชัน AI สำหรับสรุปบทเรียนและสร้างแบบทดสอบ สำหรับนักเรียนชั้นประถมศึกษาปีที่ 4-6 (อายุ 10-12 ปี)

## ✨ ฟีเจอร์

### 📚 สรุปบทเรียนอัจฉริยะ
- อัปโหลดไฟล์ (PDF, TXT, รูปภาพ) หรือพิมพ์เนื้อหาบทเรียน
- AI ค้นคว้าและขยายความให้เนื้อหาสมบูรณ์
- แบ่งเป็นหัวข้อย่อยที่อ่านง่าย 2-4 ย่อหน้าต่อหัวข้อ
- ยกตัวอย่างในชีวิตจริงที่เด็กเข้าใจง่าย
- สร้าง Infographic SVG น่ารัก สีสันสดใส

### 📝 แบบทดสอบสนุกๆ 10 ข้อ
- คำถามชัดเจน ตัวเลือก 4 ตัวเลือก (ก, ข, ค, ง)
- มีคำใบ้ (Hint) ช่วยเมื่อตอบผิด
- ลองตอบได้ 3 ครั้งต่อข้อ
- เฉลยละเอียดพร้อมคำอธิบาย
- ระบบคะแนน + ข้อเสนอแนะตามผลลัพธ์

### 🤖 ห้องพักครู (ChatBot)
- ถามตอบแบบ Real-time กับครู AI
- อธิบายละเอียดเพิ่มเติมได้ตามที่สงสัย
- ใช้ภาษาเป็นกันเอง เหมือนพี่สอนน้อง

### 🎨 UI/UX ที่เหมาะกับเด็ก
- มาสคอตน่ารักแสดงอารมณ์ (happy, thinking, excited, sad)
- สีสันสดใส ดูเป็นมิตร
- ฟอนต์ไทย: Sarabun (body), Kodchasan (headings)
- Responsive ใช้งานได้บนมือถือและคอมพิวเตอร์

## 🏗️ Architecture

**Tech Stack:**
- React 19 + TypeScript
- Vite (Build tool)
- Tailwind CSS (CDN)
- Google Gemini API (`gemini-3-flash-preview`)

**Screen Flow:**
```
UPLOAD → PROCESSING → SUMMARY → QUIZ → RESULT
          ↓              ↓
       AI Summary    ChatBot
         + Quiz
```

## 🚀 การติดตั้ง

### Prerequisites
- Node.js 18+
- Google Gemini API Key

### 1. Install Dependencies
```bash
npm install
```

### 2. Setup Environment
สร้างไฟล์ `.env.local` และใส่:
```bash
GEMINI_API_KEY=your_api_key_here
```

### 3. Run Development Server
```bash
npm run dev
```

เปิด http://localhost:3000

## 📦 Build Commands

```bash
npm run build        # Production build
npm run preview      # Preview production build
```

## 🐳 Docker Setup

### Prerequisites
- Docker
- Docker Compose

### 1. Create .env file
```bash
cp .env.example .env
```

และแก้ไขใส่ Gemini API Key:
```bash
GEMINI_API_KEY=your_gemini_api_key_here
```

### 2. Run with Docker Compose
```bash
docker compose up -d
```

แอปจะทำงานที่ http://localhost:3000

### Docker Commands
```bash
docker compose up -d              # Start containers
docker compose down               # Stop containers
docker compose logs -f            # View logs
docker compose up -d --build      # Rebuild & start
```

### Docker Volumes
- `kruai-uploads` - เก็บไฟล์อัปโหลด
- `kruai-data` - เก็บข้อมูล

## 🌐 ngrok Tunnel

ต้องการเปิดใช้งานผ่าน ngrok:

1. เพิ่ม `NGROK_AUTHTOKEN` ใน `.env`:
```bash
NGROK_AUTHTOKEN=your_ngrok_authtoken_here
```

2. Run พร้อม ngrok:
```bash
docker compose --profile tunnel up -d
```

3. ดู URL จาก ngrok:
```bash
docker compose logs ngrok
# หรือเปิด http://localhost:4040
```

## 📂 โครงสร้างไฟล์

```
KruTor/
├── App.tsx                  # Main component & state management
├── services/
│   └── geminiService.ts     # Gemini API integration
├── components/
│   ├── Button.tsx           # Reusable button
│   ├── ChatBot.tsx          # Teacher chat interface
│   └── Mascot.tsx           # SVG mascot component
├── types.ts                 # TypeScript interfaces
├── vite.config.ts           # Vite configuration
└── tailwind.config.js       # Tailwind configuration
```

## 🎯 วิธีใช้งาน

### Step 1: อัปโหลดบทเรียน
- คลิกเพื่อเลือกไฟล์ (PDF, TXT, รูปภาพ)
- หรือพิมพ์เนื้อหาบทเรียนในช่องว่าง
- กดปุ่ม "🚀 เริ่มเรียนรู้กันเลย!"

### Step 2: อ่านสรุป
- อ่านบทนำ และหัวข้อย่อยที่ AI สร้าง
- ดูตัวอย่างในชีวิตจริง
- ดู Infographic ช่วยจำ
- อ่านประเด็นสำคัญ
- **ถามครู AI** ได้ที่ "ห้องพักครู"
- กดปุ่ม "📝 ไปทำแบบทดสอบ"

### Step 3: ทำแบบทดสอบ
- อ่านคำถามและเลือกคำตอบ
- ถ้าตอบผิด จะมีคำใบ้ให้
- ลองตอบได้สูงสุด 3 ครั้ง
- เช็คเฉลยและคำอธิบาย

### Step 4: ดูผลสอบ
- ดูคะแนนรวม
- อ่านคำแนะนำจากครู AI
- ทบทวนบทเรียนอีกครั้ง
- หรือเริ่มบทเรียนใหม่

## 🔑 ข้อมูลหลักสูตร

| ประเภท | คำอธิบาย |
|--------|----------|
| **หัวข้อ** | รองรับทุกวิชา (วิทยาศาสตร์, คณิตศาสตร์, ภาษาไทย, สังคมศึกษา, ฯลฯ) |
| **ความยาว** | แนะนำ 500-10,000 ตัวอักษรต่อบท |
| **ไฟล์** | PDF, TXT, JPG, PNG, GIF, WEBP (สูงสุด 50MB) |

## 🛠️ AI Configuration

**Model:** `gemini-3-flash-preview`

**API Endpoints:**
- `generateSummary()` - สร้างสรุปบทเรียน + Infographic
- `generateQuiz()` - สร้างแบบทดสอบ 10 ข้อ
- `chatWithTeacher()` - ตอบคำถามนักเรียน

**Response Format:** JSON Schema (validated)

## 📝 License

MIT License

## 👥 Credits

สร้างด้วย ❤️ สำหรับนักเรียนไทย

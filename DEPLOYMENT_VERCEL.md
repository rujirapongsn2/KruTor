# วิธีการ Deploy KruAI ขึ้น Vercel 🚀

เนื่องจากแอปพลิเคชันนี้ใช้ฐานข้อมูล PostgreSQL และ AI (Gemini) การ Deploy บน Vercel จึงต้องมีการเตรียมการเล็กน้อยดังนี้ครับ:

## 1. เตรียมฐานข้อมูล (PostgreSQL)
Vercel ไม่มีฐานข้อมูลในตัว (ยกเว้น Vercel Postgres ที่ต้องเสียเงินหรืออยู่ในโควตา) แนะนำให้ใช้ **[Neon.tech](https://neon.tech/)** ซึ่งฟรีและตั้งค่าง่ายมาก:
1. สมัครสมาชิก Neon.tech
2. สร้าง Project ใหม่ เลือกภูมิภาคที่ใกล้ที่สุด (เช่น Singapore)
3. คัดลอก **Connection String** (ตัวอย่าง: `postgresql://root:password@ep-lucky-water-123.ap-southeast-1.aws.neon.tech/neondb?sslmode=require`)
4. **สำคัญ:** รันไฟล์ `backend/init_db.js` เพื่อสร้าง Table ในฐานข้อมูลใหม่ โดยเปลี่ยน `DATABASE_URL` ในเครื่องคุณให้ชี้ไปที่ Neon ก่อน (หรือกดรัน SQL ใน Neon Console โดยตรง)

## 2. ตั้งค่า Environment Variables ใน Vercel
เมื่อนำโปรเจกต์ขึ้น Vercel แล้ว ให้ไปที่ **Settings > Environment Variables** และเพิ่มค่าต่อไปนี้:

| Name | Value | Description |
| :--- | :--- | :--- |
| `GEMINI_API_KEY` | `your-gemini-key` | คีย์สำหรับเรียก Gemini AI |
| `DATABASE_URL` | `your-neon-url` | URL ฐานข้อมูลจาก Neon (ต้องลงท้ายด้วย `?sslmode=require`) |
| `NODE_ENV` | `production` | ระบุโหมดการทำงาน |

## 3. การ Build & Deploy
Vercel จะตรวจจับโปรเจกต์อัตโนมัติ:
- **Framework Preset:** Vite
- **Build Command:** `npm run build`
- **Output Directory:** `dist`

## 4. โครงสร้างไฟล์ที่เตรียมไว้ให้แล้ว
ผมได้สร้างไฟล์ `vercel.json` ไว้ที่ Root ของโปรเจกต์เพื่อ:
- จัดการ Routing ให้ `/api` วิ่งไปที่ Express Backend
- จัดการ Build ของทั้ง Frontend และ Backend ให้ทำงานร่วมกันได้ใน Serverless Environment

---
**หมายเหตุ:**
- **Timeout:** Vercel แผนฟรีมี Timeout ของ Serverless Functions อยู่ที่ 10 วินาที หาก Gemini ประมวลผลนานกว่านั้นอาจเกิด Error 504 ได้ (แนะให้ใช้ Gemini Flash ซึ่งมักจะตอบกลับภายในเวลาที่กำหนด)
- **SSL:** การเชื่อมต่อ Database จาก Vercel ต้องใช้ SSL เสมอ (Neon จัดการให้แล้ว)
